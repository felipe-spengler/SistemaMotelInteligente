-- BANCO DE DADOS: u876938716_motel
-- MUDANÇAS PARA A NOVA VERSÃO DO SISTEMA (PERÍODOS DINÂMICOS E CATEGORIAS)

-- 1. Cria a tabela de periodos por quarto
CREATE TABLE IF NOT EXISTS `periodos_quarto` (
  `id` int NOT NULL AUTO_INCREMENT,
  `numeroquarto` int NOT NULL,
  `descricao` varchar(50) NOT NULL,
  `tempo_minutos` int NOT NULL,
  `valor` float NOT NULL,
  `is_pernoite` tinyint(1) DEFAULT '0',
  `ordem` int NOT NULL,
  PRIMARY KEY (`id`),
  KEY `fk_periodos_quarto` (`numeroquarto`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 2. Adiciona a coluna para saber qual periodo o cliente usou na locacao (para os periodos dinamicos)
ALTER TABLE `registralocado` ADD COLUMN `periodo_locado` VARCHAR(50) DEFAULT NULL;

-- 3. Adiciona categorias na tabela de produtos para montar o cardápio digital
UPDATE configuracoes SET pedidos_online = 0;
ALTER TABLE produtos ADD COLUMN categoria VARCHAR(100) DEFAULT 'Diversos';
ALTER TABLE produtos ADD COLUMN imagem VARCHAR(255) DEFAULT NULL;
ALTER TABLE produtos ADD COLUMN detalhes TEXT DEFAULT NULL;

-- 4. Suporte a Pedidos Online via Cardápio
CREATE TABLE IF NOT EXISTS `pedidos_online` (
  `id` int NOT NULL AUTO_INCREMENT,
  `numeroquarto` int NOT NULL,
  `itens` text NOT NULL, -- JSON ou lista de itens: "2x Coca, 1x X-Tudo"
  `valor_total` float DEFAULT 0,
  `status` varchar(20) DEFAULT 'pendente', -- pendente, visualizado, entregue, cancelado
  `hora_pedido` timestamp DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

ALTER TABLE `configuracoes` ADD COLUMN `pedidos_online` tinyint(1) DEFAULT '0';
